info:
  name: openapi.json
  type: http
  seq: 1

http:
  method: GET
  url: https://soarscore2-quiet-shadow-5419.fly.dev/openapi/v1.json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
