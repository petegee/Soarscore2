<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>$(SoarscoreTargetFramework)</TargetFramework>
    <RootNamespace>Soarscore.Infrastructure</RootNamespace>
  </PropertyGroup>

  <!-- LADR-0001: Marten on PostgreSQL is the only event-store adapter we build.
       This is the one project allowed to reference Marten (§4.1-3) — the
       IProjection shims here wire Application's plain fold functions into
       Marten's Live/Inline aggregation; the fold logic itself stays here-free
       of Marten types so a future SQLite adapter could reuse it unchanged. -->

  <ItemGroup>
    <PackageReference Include="Marten" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Soarscore.Application\Soarscore.Application.csproj" />
  </ItemGroup>

</Project>
